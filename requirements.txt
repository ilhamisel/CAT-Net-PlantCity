# CAT-Net-PlantCity — Python package requirements
# Tested versions on Python 3.13.5 (Anaconda) + CUDA 12.6 + RTX 3090.
# Older/newer minor versions should work; pin if reproducing exact numbers.

# ---------- Deep learning core ----------
torch==2.8.0
torchvision==0.23.0
timm==1.0.24

# ---------- Numerics, science, plotting ----------
numpy>=2.0
scipy>=1.13
scikit-learn>=1.5
matplotlib>=3.9
pandas>=2.2

# ---------- Image IO ----------
Pillow>=11
opencv-python>=4.10

# ---------- Explainability ----------
grad-cam==1.5.5

# Notes:
# * For CUDA support, install torch/torchvision wheels matching your CUDA toolkit:
#       pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu126
# * Inference and the paper_v2 pipeline run on CPU, but training assumes CUDA.
